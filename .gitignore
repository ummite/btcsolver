# Rust build artifacts
/target

# Compiled binaries
*.exe
*.pdb
*.dSYM

# Cargo lock
Cargo.lock

# Build/check logs
check.log
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Monitoring / utility scripts (generated during sync)
check_*.ps1
monitor_sync.ps1
now_check.ps1
quick_check.ps1

# Bitcoin data (ne jamais committer !)
*.cookie
bitcoin.conf
*.redb

# UTXO cache / index (générés localement, souvent multi-GB)
utxo-cache.bin
utxo-index.redb
utxo-index.redb.*
utxo-index.redb.bak*
utxo-index.snapshot
utxo-index.snapshot.*
*.redb.bak*
*.snapshot.bak
*.bak

# Brute-force runtime files (générés à l'exécution)
brute-force-stats.json
brute-force-stats-*.json
brute-force-progress.json
brute-force-progress-*.json
found-keys.json
brute-force-error.log

# Brainwallet / bible pattern outputs (générés, peuvent être énormes)
bible-patterns*.txt
bible-brainwallet*.txt
bible-brainwallet*.json
brainwallet-*-results.json
brainwallet-*-corpus*.txt
brainwallet-all*.txt
brainwallet-matches.json
brainwallet-extended-matches.json
brainwallet-bible-matches.json
*-matches.json

# CUDA build artifacts (recompiled per GPU architecture)
libsecp_gpu.dll
libsecp_gpu.exp
libsecp_gpu.lib
*.cu.backup
